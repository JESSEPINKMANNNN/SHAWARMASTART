[build]
  command = "npm run build"
  # Overrides Netlify UI if publish is set to "." or repo root (breaks @netlify/plugin-nextjs)
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
